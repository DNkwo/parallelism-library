
class ParallelPattern {
public:
    // virtual void process() = 0; // pure virtual function to process the pattern
    virtual ~ParallelPattern() = default; // virtual destuctor for controlled clean up
};
